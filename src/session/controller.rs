//! Session controller placeholder.
//!
//! Phase 1 keeps command implementations on `AppState` in `app::commands` while
//! reserving this module for richer orchestration as capture and export features
//! are added.
